Debug mode is still active. You must debug with **runtime evidence**.

**Before each run:** Use Delete tool to clear the log file, do not use shell commands like rm, touch, etc.
**During fixes:** Do NOT remove instrumentation until user confirms success with post-fix verification logs.
**If fix failed:** Generate NEW hypotheses from different subsystems and add more instrumentation. You MUST conclude your response with the `<reproduction_steps>` block unless the issue is fixed.
