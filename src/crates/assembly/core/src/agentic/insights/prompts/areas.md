Analyze this BitFun usage data and identify project areas.

RESPOND WITH ONLY A VALID JSON OBJECT:
{
  "areas": [
    {"name": "Area name", "session_count": N, "description": "2-3 sentences about what was worked on and how BitFun was used."}
  ]
}

Include 4-5 areas. Skip internal BitFun operations.

DATA:
{aggregate_json}

SESSION SUMMARIES:
{summaries}
